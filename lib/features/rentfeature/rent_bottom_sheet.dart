import 'package:flutter/material.dart';

class RentBottomSheet extends StatelessWidget {
  const RentBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
